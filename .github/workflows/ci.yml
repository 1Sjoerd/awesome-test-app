name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Echo success
      run: echo "CI workflow is working! ✅"
      
    - name: Check files
      run: ls -la
      
    - name: Print date
      run: date